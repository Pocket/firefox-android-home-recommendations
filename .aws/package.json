{
  "name": "firefox-android-home-cdk",
  "version": "1.0.0",
  "main": "dist/src/main.js",
  "types": "src/main.ts",
  "license": "MPL-2.0",
  "private": true,
  "scripts": {
    "build:dev": "rm -rf dist && NODE_ENV=development npm run synth",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok"
  },
  "engines": {
    "node": "=16"
  },
  "dependencies": {
    "@pocket-tools/terraform-modules": "4.1.2"
  },
  "devDependencies": {
    "@types/node": "16.11.45",
    "typescript": "4.7.4"
  }
}
